/**
 * @File: based on HTML5 WebSocket encapsulation,
 * support params serialization, heatbeat, reconnect.
 */
import _ from 'lodash';

class BiciWebSocket {
  constructor(options) {
    this.options = options;
  }
}

export default BiciWebSocket;
